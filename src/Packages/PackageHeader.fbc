file_identifier	"ECRF";
file_extension	".e2toc";

namespace Eldritch2.FileSystem.FlatBuffers;

table Export {
	Name	: string (required);
	Type	: string (required);
	Offset	: ulong;
	Length	: ulong;
}

table PackageHeader {
	Imports : [string];
	Exports : [Export];
}

root_type PackageHeader;